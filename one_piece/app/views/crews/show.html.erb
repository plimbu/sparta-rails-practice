<p>
  <strong>Name:</strong>
  <%= @crew.name %>
</p>

<p>
  <strong>Location:</strong>
  <%= @crew.location %>
</p>

<p>
  <strong>Characters:</strong>

    <% @crew.characters.each do |character| %>
      <%= character.name %>
      <%= character.power %>
      <%= character.crew.name %>
    <% end %>

</p>

<%= link_to 'Edit', edit_crew_path(@crew) %> |
<%= link_to 'Back', crews_path %>
